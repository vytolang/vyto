<?xml version="1.0" encoding="utf-8"?>
<!-- The window background during launch: a flat colour, deliberately.
     A layer-list with a centred logo is the obvious next step and is left to
     the app, because the drawable an app supplies with this name wins. -->
<color xmlns:android="http://schemas.android.com/apk/res/android"
       android:color="@color/vyto_splash_bg" />
