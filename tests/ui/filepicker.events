# pickdir (folders first, dotfiles hidden, no ".."): sub, a.txt, b.txt
# selection starts at 0 = "sub" (a directory); Enter descends into sub/
key Enter
# sub/ has only inner.txt -> selection 0; Enter accepts it
key Enter
