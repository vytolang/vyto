// Tagging is all-or-nothing: an untagged variant here would serialize as its
// own identifier while its siblings use their strings.
enum Header { ContentType = "content-type", XReal }

fn main() {
    print(Header.XReal.name());
}
