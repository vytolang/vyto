// '{' and '}' are symmetric inside a template: both must be doubled to appear
// literally. A bare '}' is a typo often enough that accepting it silently is
// worse than the error.
fn main() {
    let s = `a}b`;
    print(s);
}
