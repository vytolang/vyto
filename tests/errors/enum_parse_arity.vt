// parse() and has() ask the type about a name, so they need exactly that name.
// Calling parse() with nothing is the mistake this catches.
enum Color { Red, Green }

fn main() {
    let c = Color.parse();
    print(c.name());
}
