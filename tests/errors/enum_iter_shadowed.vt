// A local named like the enum wins, matching how Color.Red resolves: the
// iterator is then an ordinary value, and an int is not iterable.
enum Color { Red, Green, Blue }

fn main() {
    let Color = 3;
    for (let c in Color) { print("x"); }
}
