// cstring is C-shaped and outside the string model, so it needs an explicit
// str() the same way it does everywhere else. The error says so.
extern "C" {
    fn getenv(name: cstring): cstring;
}

fn main() {
    let p = getenv("PATH".cstr());
    print(`path is {p}`);
}
