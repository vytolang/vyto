// A const initializer has to fold at compile time; a template with a hole does
// not. (A hole-free template is just a literal and is fine as a const.)
const S: string = `a{1}b`;

fn main() {
    print(S);
}
