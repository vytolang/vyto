// A bound method's type excludes the receiver, so the arity is the method's own.
class App { fn hi(a: int, b: string) { print(a + b); } }
fn take(f: fn(int)) { f(1); }
fn main() { let a = new App(); take(a.hi); }
