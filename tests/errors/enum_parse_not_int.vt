// parse() returns the enum, not its ordinal. If it handed back a bare int the
// feature would reintroduce exactly the untyped-tag problem enums exist to fix.
enum Color { Red, Green }

fn main() {
    let n: int = Color.parse("Red");
    print(str(n));
}
