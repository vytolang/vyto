// An enum value has exactly one method. Anything else names something that
// does not exist rather than silently doing nothing.
enum Color { Red, Green }

fn main() {
    print(Color.Red.upper());
}
