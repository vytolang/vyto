// The import matches no local file and no registered root. The diagnostic
// lists every root that was searched, so the first line ends with the colon.
import { nothing } from "no/such/package";

fn main() { nothing(); }
