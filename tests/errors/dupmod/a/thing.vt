// Same stem as ../b/thing.vt, and neither is under a search root, so both are
// named by their bare stem 'thing'. Helper for dup_module_name.vt; the runner
// globs tests/errors/*.vt flat, so nothing here is collected as a case.
export fn a_thing(): string { return "a"; }
