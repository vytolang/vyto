// An enum is not a number. Bitflags are a separate feature, so arithmetic on a
// variant is rejected rather than half-supported.
enum Color { Red, Green }

fn main() {
    let c = Color.Red;
    print(str((c + 1) as int));
}
