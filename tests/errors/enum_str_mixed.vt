// A string says how a variant serializes; an integer says what it is worth.
// An enum answers one question or the other, never half of each.
enum Status { Active = "active", Idle = 2 }

fn main() {
    print(Status.Active.name());
}
