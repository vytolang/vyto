// Two variants with one spelling would make parse() ambiguous and .name()
// lossy, so it is rejected the way a duplicate ordinal is.
enum Status { Active = "on", Busy = "on" }

fn main() {
    print(Status.Active.name());
}
