// "gamma/config" is not beside this file and not in the stdlib, so this only
// builds when ../pkgs is supplied as a root by --modpath or $VYTO_PATH.
import { name } from "gamma/config";

fn main() {
    print(name());
}
