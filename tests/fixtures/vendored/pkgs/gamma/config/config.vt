// Deliberately NOT under a vyto_modules/, so this package is reachable only
// via --modpath or $VYTO_PATH. Both name it gamma_config -- identical to what
// vendoring would produce, which is what makes the two routes interchangeable.
export fn name(): string {
    return "gamma config";
}
