// `join` is a builtin on string[] only, so an int[] needs one line of its own.
export fn fmt_ints(xs: int[]): string {
    let out = "";
    for (let i in 0..xs.len) {
        if (i > 0) { out = out + ","; }
        out = out + str(xs[i]);
    }
    return out;
}
