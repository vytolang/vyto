// Exercises the stdlib search path:
// - "volt/hello" resolves via $VOLT_HOME/lib (package layout, name-mangled module)
// - a user module with the same leaf name ("hello") coexists
import { lib_greet } from "volt/hello";
import { user_greet } from "hello";

fn main() {
    print(lib_greet());
    print(user_greet());
}
