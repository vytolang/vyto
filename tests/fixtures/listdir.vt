// Direct check of the listdir / isdir builtins over tests/tmp/pickdir.
fn main() {
    for (let e in listdir("tests/tmp/pickdir")) {
        let tag = "file";
        if (isdir("tests/tmp/pickdir/" + e)) { tag = "dir"; }
        print(e + " (" + tag + ")");
    }
}
