// Shadows vyto_modules/alpha/config. Under no root, so it is named by its
// bare stem 'config' -- and the package copy it shadows is never loaded, so
// there is nothing for it to collide with.
export fn name(): string {
    return "shadowing alpha config";
}
