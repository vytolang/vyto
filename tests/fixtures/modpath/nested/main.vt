// Same imports as ../main.vt, but this directory has no vyto_modules/ of its
// own: the root is found by walking UP to tests/fixtures/modpath/.
import { alpha_name } from "alpha/config";
import { beta_name } from "beta/config";

fn main() {
    print(alpha_name());
    print(beta_name());
}
