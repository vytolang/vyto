// Used by run_tests.sh: overflows i64 max + 1.
// Debug build must PANIC (checked); --release must WRAP to i64 min.
fn main() {
    let m: int = 9223372036854775807;
    let one: int = 1;
    print("" + (m + one));
}
