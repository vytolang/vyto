// Conditional #link: the windows-only library must NOT reach the linker
// on a Linux build, or this program could never link.
#link "volt_no_such_library" if "windows"
#link "m" if "linux"

fn main() {
    print("conditional link ok");
}
