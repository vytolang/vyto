// Hello world, variables, string concat, for-range.
fn main() {
    print("hello, vyto");
    let n = 3;
    for (let i in 0..n) {
        print("tick " + i);
    }
    let x: float = 2.5;
    print("x*2 = " + x * 2.0);
    print("done: " + true);
}
