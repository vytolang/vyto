vytoc
vytobind
CLAUDE.local.md
.vyto-cache/
.claude
.codegraph
.local
local
docs-site
docs-local
*.o
*.so
*.a
*.dll
# macOS shared libraries, for the same reason as *.dll and *.so: a provisioned
# libssl.dylib / libcrypto.dylib lands in lib/vyto/crypto/openssl/native/<triple>/
# and must ship with a release rather than sit in git.
*.dylib
*.exe
*.crt
# ...except the ones examples/107_tls.vt is a test OF. They are throwaway test
# certificates whose private keys sit beside them in the open; nothing trusts
# them and nothing may reuse them. They are committed because 107_tls.vt runs in
# `make test`, and a suite that generates its own key material needs openssl(1)
# on the machine — which would make it a test of the CLI rather than of the
# bindings, and would silently skip wherever the CLI is missing.
!tests/fixtures/tls/*.crt
examples/greeter/greeter.vt
examples/greeter/native/
tests/xsend
cbwrap.vt
benchmarks/*_c
benchmarks/*_vyto
benchmarks/*_volt
benchmarks/logs.db
benchmarks/sample_large.log
# vendored SQLite amalgamation for 08_sqlite — fetched, not committed (see benchmarks/README.md)
benchmarks/native/src/sqlite3.c
benchmarks/native/src/sqlite3.h
tests/tmp/

# app build artifacts (built by tests via -o, or manually)
apps/chip8/chip8

# vyto/gfx: blend2d prebuilt dep (populated by build-blend2d.sh, not vendored)
lib/vyto/gfx/native/src/blend2d/
lib/vyto/gfx/native/*/libblend2d.*

# Provisioned per-triple headers for prebuilt native deps. vytoc searches
# native/<triple>/include ahead of native/src; each package's native/*.sh
# regenerates these, so they are never vendored.
lib/vyto/**/native/*/include/

# vyto/net: libcurl for windows-x64 (see native/provision-curl.sh). The DLL
# itself is shipped with a Vyto release, not carried in git.
lib/vyto/net/native/*/libcurl*
lib/vyto/net/native/*/curl-ca-bundle.crt

# built calc app binary
apps/calc/vytocalc

# apps/hn_android: apk pipeline output (manifest, dex, .so, signed apk). Built
# by apps/hn_android/build-apk.sh, which needs an NDK and an SDK — no test
# suite runs it, so this directory only exists on a machine that has both.
apps/hn_android/build/
# Android build-verification: local only, so GitHub CI never has to download an
# NDK or an SDK. Needs ~/Android/{Ndk,Sdk} (or $ANDROID_NDK_HOME/$ANDROID_SDK_HOME).
tests/run_tests_android.sh
tests/android_jni_check.py
# PostgreSQL live checks: local only, so GitHub CI never has to stand up a
# database. Needs $VYTO_PG_DSN pointing at a real server; skips cleanly without
# one. The FIXTURES it runs are committed — examples/105_pgwire.vt covers the
# same driver offline, and these are what a live server adds on top.
tests/run_tests_pgsql.sh
tests/run_tests_mysql.sh

# Signing keys never belong in the repo. build-apk.sh keeps the debug key in
# local/mobile/ (already ignored); this catches a stray one anywhere else.
*.keystore
*.jks
