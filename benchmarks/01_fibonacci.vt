// Benchmark 1: Recursive Fibonacci
fn fib(n: int): int {
    if (n <= 1) { return n; }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let n = 40;
    let result = fib(n);
    print("fib(" + n + ") = " + result);
}
