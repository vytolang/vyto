# The store and everything generated from it. A default run is ~450 MB and the
# --rows 10000000 run is ~4.5 GB, plus the WAL and the HTML dumps that
# /t/:name/all writes. It is data, not source, and never belongs in the
# repository — same call apps/jpl makes for its 116 MB catalogue.
data/
