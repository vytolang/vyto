# Downloaded SBDB catalogues. The full one is ~116 MB and JPL serves it at
# ~200 KB/s, so it is cached here rather than re-fetched — but it is data, not
# source, and never belongs in the repository.
*.json
data/
